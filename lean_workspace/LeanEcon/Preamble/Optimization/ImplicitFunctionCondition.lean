import Mathlib

/- Implicit function theorem template for comparative statics.

The usual target is an algebraic identity of the form
`dx/dθ = -(∂F/∂θ) / (∂F/∂x)` once derivatives have been introduced as
hypotheses. -/
